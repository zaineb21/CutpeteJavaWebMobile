{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPanier</th>
                <td>{{ panier.idPanier }}</td>
            </tr>
            <tr>
                <th>QuantitePanier</th>
                <td>{{ panier.quantitePanier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_front_panier_index') }}">back to list</a>

    <a href="{{ path('app_front_panier_edit', {'idPanier': panier.idPanier}) }}">edit</a>

    {{ include('front_panier/_delete_form.html.twig') }}
{% endblock %}
