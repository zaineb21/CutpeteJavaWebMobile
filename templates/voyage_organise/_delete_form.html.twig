<form method="post" action="{{ path('voyage_organise_delete', {'id': voyage_organise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voyage_organise.id) }}">
    <button class="btn">Delete</button>
</form>
