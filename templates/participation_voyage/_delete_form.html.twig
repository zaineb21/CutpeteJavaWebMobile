<form method="post" action="{{ path('participation_voyage_delete', {'id': participation_voyage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participation_voyage.id) }}">
    <button class="btn">Delete</button>
</form>
