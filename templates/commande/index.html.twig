


{% extends 'baseBack.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande index</h1>

    <table class="table">
        <thead>
        <tr>
            <th> id Utulisateur</th>
            <th> Nom Utulisateur</th>
            <th>IdCommande</th>
            <th>AdresseCommande</th>
            <th>Etat</th>
            <th> changer Etat Commande</th>

        </tr>
        </thead>
        <tbody>

        {% for commande in commandes %}
            <tr>
                <td>{{commande.idUtulisateur.idUtilisateur}} </td>
                <td> {{commande.idUtulisateur.nom}} </td>
                </td>
                <td>{{ commande.idCommande }}</td>
                <td>{{ commande.adresseCommande }}</td>
                <td>{{ commande.etatCommande }}</td>
<td>      <a href="{{path("updateEtat",{'idCommande':commande.idCommande})}}"
             class="btn  btn-warning"> supprimer  quantité
        <i class="bi bi-cart-plus"></i>
    </a></td>

                <td>


                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}

