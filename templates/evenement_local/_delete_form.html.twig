<form method="post" action="{{ path('evenement_local_delete', {'id': evenement_local.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement_local.id) }}">
    <button class="btn">Delete</button>
</form>
