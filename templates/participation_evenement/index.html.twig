{% extends 'base.html.twig' %}

{% block title %}ParticipationEvenement index{% endblock %}

{% block body %}
    <h1>ParticipationEvenement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participation_evenement in participation_evenements %}
            <tr>
                <td>{{ participation_evenement.id }}</td>
                <td>
                    <a href="{{ path('participation_evenement_show', {'id': participation_evenement.id}) }}">show</a>
                    <a href="{{ path('participation_evenement_edit', {'id': participation_evenement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('participation_evenement_new') }}">Create new</a>
{% endblock %}
