{% extends 'base.html.twig' %}

{% block title %}ParticipationEvenement{% endblock %}

{% block body %}
    <h1>ParticipationEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participation_evenement.id }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ participation_evenement.idUser }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ participation_evenement.idEvent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('participation_evenement_index') }}">back to list</a>

    <a href="{{ path('participation_evenement_edit', {'id': participation_evenement.id}) }}">edit</a>

    {{ include('participation_evenement/_delete_form.html.twig') }}
{% endblock %}
