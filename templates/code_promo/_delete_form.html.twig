<form method="post" action="{{ path('app_code_promo_delete', {'idCodepromo': code_promo.idCodepromo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code_promo.idCodepromo) }}">
    <button class="btn">Delete</button>
</form>
